/*--------------------------------------------- 
    This file imports the createBrowserHistory() method of the history module
    and the exports it in a constant for simplicity

/*---------------------------------------------- */

import {createBrowserHistory} from 'history';

const history = createBrowserHistory();

export default history;